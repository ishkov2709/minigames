import styled from '@emotion/styled';

export const Header = styled.header`
  padding: 50px 0;
`;

export const List = styled.ul`
  display: flex;
  gap: 24px;

  margin: 0;
  padding: 0;

  list-style: none;
`;
